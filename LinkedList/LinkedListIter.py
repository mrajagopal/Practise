#!/usr/bin/env python
# 2021-04-11: ST. Iterator edition

import math

class Element:

    def __init__(self, val, isHead=False):
        self.isHead = isHead:
        self.value = val                                       # The value of the element

        if self.isHead ==

        self.next = None                                       # Next Element



if __name__ == '__main__':
    FirstElement = Element(0, )
